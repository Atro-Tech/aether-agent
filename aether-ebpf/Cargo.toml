[package]
name = "aether-ebpf"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Æther Agent eBPF probes — hallucinator (credential swap) and Shimmer (file I/O monitoring)"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
