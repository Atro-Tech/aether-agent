[package]
name = "aether-fs"
version = "0.4.0"
edition = "2021"
license = "Apache-2.0"
description = "FUSE overlay for Æther Agent. Mounts over /usr/bin, /usr/lib, /opt. Lazy file loading."

[dependencies]
anyhow = "1"
slog = "2.5"

[dev-dependencies]
tempfile = "3"
