[package]
name = "aether-fs"
version = "0.3.0"
edition = "2021"
license = "Apache-2.0"
description = "AgentFS — the root filesystem. FUSE. Three layers. Put files in, they appear."

[dependencies]
anyhow = "1"

[dev-dependencies]
tempfile = "3"
